
<%= form_for @new_c do |f| %>
	<%= f.hidden_field :commentable_id, :value => @new_c.commentable_id %>
	<%= f.hidden_field :commentable_type, :value => @new_c.commentable_type %>
	<%= f.label :"Cuerpo de Comentario: " %>
	<%= f.text_area :body %>
	<%= f.submit 'Guardar' %>
<% end %>