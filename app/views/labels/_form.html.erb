<%= form_for @label do |f| %>
	<%= f.label :"Titulo: " %>
	<%= f.text_field :title , placeholder: "Ingrese el titulo aqui"%>
	<%= f.label :Estado %>
	<%= f.check_box :active%>
	<%= f.submit 'Guardar' %>
<% end %>