<%= form_for(@question) do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>
	<br>
	<%= f.label :body %>
	<%= f.text_area :body %>
	<br>
	<%= f.label :labels %>
	<%= f.collection_check_boxes :label_ids,@all_labels,:id,:title %>
	<br>
	<%= f.submit 'Enviar' %>
<% end %>